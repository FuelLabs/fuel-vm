name: CI

on:
  push:
    branches:
      - master
  pull_request:
  merge_group:
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_VERSION: 1.67.0

jobs:
  cargo:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - command: fmt
            args: --all -- --check
          - command: clippy
            args: --all-targets --all-features -- -D warnings -D clippy::dbg_macro
          # do regular check of all crates
          - command: make
            args: check
          # check all crates with no default features
          - command: make
            args: check --no-default-features
          # check all crates with all features enabled
          - command: make
            args: check --all-features
          # test no-std builds
          - command: check
            args: --target thumbv6m-none-eabi -p fuel-asm -p fuel-storage -p fuel-merkle --no-default-features
          # test wasm build of fuel-crypto
          - command: check
            args: --target wasm32-unknown-unknown -p fuel-crypto --no-default-features
          # test all crates with all features enabled
          - command: test
            args: --all-targets --all-features
          # test each crate with no features enabled
          - command: make
            args: test --no-default-features
          # test each crate with serde enabled
          - command: make
            args: test --all-targets --no-default-features --features serde
          # test each crate with alloc enabled
          - command: make
            args: test --all-targets --no-default-features --features alloc
          # test each crate with random enabled
          - command: make
            args: test --all-targets --features random
          # test each crate with serde enabled
          - command: make
            args: test --all-targets --features serde
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: "thumbv6m-none-eabi,wasm32-unknown-unknown"
          components: "clippy, rustfmt"
      - name: Install Cargo Make
        uses: davidB/rust-cargo-make@v1
        with:
          version: "0.36.4"
      - run: cargo ${{ matrix.command }} ${{ matrix.args }}

  cargo-toml-fmt-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_VERSION }}
      - name: Install Cargo.toml linter
        uses: baptiste0928/cargo-install@v1
        with:
          crate: cargo-toml-lint
          version: "0.1"
      - name: Run Cargo.toml linter
        run: git ls-files | grep Cargo.toml$ | xargs --verbose -n 1 cargo-toml-lint

  verifications-complete:
    needs:
      - cargo
      - cargo-toml-fmt-check
    runs-on: ubuntu-latest
    steps:
      - run: true

  publish-check:
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Publish crate check
        uses: katyo/publish-crates@v2
        with:
          dry-run: true

  publish:
    # Only do this job if publishing a release and all checks pass.
    needs:
      - verifications-complete
      - publish-check
    if: github.event_name == 'release' && github.event.action == 'published'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Verify tag version
        run: |
          curl -sSLf "https://github.com/TomWright/dasel/releases/download/v1.24.3/dasel_linux_amd64" -L -o dasel && chmod +x dasel
          mv ./dasel /usr/local/bin/dasel
          ./.github/workflows/scripts/verify_tag.sh ${{ github.ref_name }} Cargo.toml

      - name: Publish crate
        uses: katyo/publish-crates@v2
        with:
          registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
