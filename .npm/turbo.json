{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "outputMode": "new-only"
    },
    "test": {
      "outputMode": "new-only"
    }
  }
}
