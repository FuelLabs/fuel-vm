{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "outputLogs": "new-only"
    },
    "test": {
      "outputLogs": "new-only"
    }
  }
}
