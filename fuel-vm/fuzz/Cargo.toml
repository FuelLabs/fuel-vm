[package]
name = "fuel-vm-fuzz"
version = "0.0.0"
authors = ["Automatically generated"]
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
arbitrary = { version = "1.0", features = ["derive"] }
fuel-vm = { path = "..", features = ["arbitrary", "test-helpers"] }
libfuzzer-sys = { version = "0.13", package = "libafl_libfuzzer" }
clap = { version = "4.0", features = ["derive"] }
hex = "*"

# Prevent this from interfering with workspaces as this crate requires unstable features.
[workspace]
members = ["."]

[profile.release]
panic = 'abort'

[profile.dev]
panic = 'abort'

[[bin]]
name = "grammar_aware_advanced"
path = "fuzz_targets/grammar_aware_advanced.rs"
test = false
doc = false
